version: 2
# Set the OS, Python version and other tools you might need

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

python:
  install:
    - method: pip
      path: ".[doc]"

sphinx:
  builder: html
  configuration: docs/conf.py
